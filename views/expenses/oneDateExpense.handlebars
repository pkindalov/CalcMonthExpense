
<div class="container">
    <div class="row">
        <div class="col-lg-12">
            {{#if expense}}
            <p>Expense maded on {{expense.formattedDate}}</p>
            <p>Products buyed on this date</p>
            {{#each expense.products}}
                <p>Product name: {{this.name}}</p>
                <p><img src={{this.photo}} alt={{this.name}} /></p>
                <p>Product price: {{this.price}}</p>
            {{/each}}

            <p>Reason for buying this: {{expense.description}}</p>
            <p>Do you really need these products? {{expense.isItAbsolutelyNeeded}}</p>
            <a class="btn btn-success" href="/expenseDetails?id={{expense._id}}">See More..</a>
        </div>

        {{else}}
            <h3>You have no expenses on this date.</h3>

        {{/if}}
    </div>
</div>